<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 填充的颜色 -->
    <solid android:color="#dddddd" />
        <!--透明 <solid android:color="@android:color/transparent" />-->
    <!-- 设置按钮的四个角为弧形 -->
    <corners android:radius="30dip" />    <!-- android:radius 弧形的半径 -->
    <stroke android:width="3px" android:color="@color/mycolor" />    <!--边框的宽度及颜色-->
</shape>